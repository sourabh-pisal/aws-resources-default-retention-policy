{
  "name": "aws-resources-default-retention-policy",
  "version": "0.1.0",
  "bin": {
    "aws-resources-default-retention-policy": "bin/aws-resources-default-retention-policy.js"
  },
  "scripts": {
    "cdk": "cdk",
    "test": "jest",
    "lint": "gts lint",
    "clean": "gts clean && rm -rf cdk.out",
    "compile": "tsc",
    "fix": "gts fix",
    "watch": "tsc -w",
    "build": "npm run compile",
    "synth": "npm run cdk synth",
    "deploy": "npm run cdk deploy",
    "prebuild": "npm run test",
    "postbuild": "npm run synth",
    "posttest": "npm run lint"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.114",
    "@types/jest": "^29.4.0",
    "@types/node": "18.14.6",
    "aws-cdk": "2.73.0",
    "gts": "^3.1.1",
    "jest": "^29.5.0",
    "ts-jest": "^29.0.5",
    "ts-node": "^10.9.1",
    "typescript": "~4.9.5"
  },
  "dependencies": {
    "@aws-cdk/aws-lambda-destinations": "^1.198.1",
    "aws-cdk-lib": "2.73.0",
    "aws-lambda": "^1.0.7",
    "constructs": "^10.0.0",
    "source-map-support": "^0.5.21"
  }
}
